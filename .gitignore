/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

# Temporary session files (generated per session, not committed)
SESSION_*.md
CURRENT_STATUS.txt
QUICK_START_*.md
setup-shadcn.ps1

# Claude Code context (auto-generated, personal workspace files)
.claude/context.md
.claude/session-context.md
claude.md


# ============================================
# Claude Code Hooks System
# Add these lines to the project's .gitignore
# ============================================

# Claude hooks and automations (PRIVATE - local only)
.claude/
!.claude/.gitkeep

# Historial y logs de sesiones (PRIVATE)
.claude/history/
.claude/locks/
.claude/kb-sync/

# Configuraciones de hooks (PRIVATE)
.claude/settings.json
.claude/settings.local.json

# Scripts de hooks (PRIVATE)
.claude/hooks/

# Registry file (session tracking)
.claude/registry.json

# Contexto personal (PRIVATE)
claude.md

# Documentación de sesiones (PRIVATE - no commitear)
SESSION_*.md
CURRENT_STATUS.md
INTEGRATION_STATUS.md
*_SUMMARY_*.md

# Permitir solo el README del proyecto
# (El README es público y va al repo)

